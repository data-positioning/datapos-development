{
    "extends": "./tsconfig.json", // Reuse the strict Node baseline.
    "compilerOptions": {
        "allowImportingTsExtensions": true, // Let tooling import '.ts' paths directly during Vite dev.
        "baseUrl": "./", // Makes relative imports like 'src/...' resolve from the repo root.
        "isolatedModules": true, // Treat each file as a separate module; keeps Vite happy.
        "lib": ["DOM", "ESNext"], // Provide DOM types alongside modern JS for browser builds.
        "moduleResolution": "bundler", // Resolve modules the way Vite/ESBuild expects.
        "module": "ESNext", // Emit plain ESNext modules for the browser bundle.
        "paths": {
            "~/*": ["*"], // Configure import aliases for root directory.
            "@/*": ["src/*"] // Configure import aliases for 'src' directory.
        }
    },
    "include": ["vite.config.*", "src/**/*"] // Type-check the Vite configuration and project sources only.
}
